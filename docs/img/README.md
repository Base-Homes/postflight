# Diagram sources

`turn-scope-light.svg` and `turn-scope-dark.svg` are generated, with their text
converted to outlines so the typeface does not depend on the viewer having it
installed. They are path data and cannot be usefully hand-edited.

The generator is not checked in. Open an issue if you need a change to the diagram.
